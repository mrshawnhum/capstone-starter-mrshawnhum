import PropTypes from "prop-types";
import React from "react";

import { FilterableImgTable as Img } from "components/FilterableImgTable";

export class DogGallery extends React.Component {
  // state = {[]}

  render() {
    return <Img />;
  }
}
